name: example
description: An example app for form_builder_image_picker
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dotted_border: ^2.0.0+1
  form_builder_image_picker:
    path: ../
  form_builder_validators: ^7.2.0
  flutter_i18n: ^0.31.0

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
